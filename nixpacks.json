{
  "phases": {
    "install": {
      "cmds": [
        "pnpm install"
      ]
    },
    "build": {
      "cmds": [
        "pnpm install",
        "pnpm --filter @workspace/api-server run build",
        "BASE_PATH=/ PORT=3000 pnpm --filter @workspace/insta-app run build"
      ]
    }
  }
}
