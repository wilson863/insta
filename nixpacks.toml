[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server run build", "BASE_PATH=/ PORT=3000 pnpm --filter @workspace/insta-app run build"]
